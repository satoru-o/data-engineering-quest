[project]
name = "dequest-quest02"
version = "0"
requires-python = "==3.12.*"

# 判定は dtype を見る。CSV に書き出すと型が文字列に潰れて判定できないので、
# 出力は Parquet にしてある。そのために pyarrow が要る。
# ノートブックは使わないクエストなので jupyterlab も ipython も入れない
dependencies = [
    "pandas==2.2.3",
    "pyarrow==18.1.0",
]

[tool.uv]
package = false
